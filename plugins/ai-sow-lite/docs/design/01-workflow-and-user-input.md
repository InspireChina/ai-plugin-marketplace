# 01 · 两个入口的流程与用户输入

[返回总览](README.md)

## 1. 两个独立请求

`generate` 首次形成 SOW；`clarify` 基于已有 SOW 处理答复、补充和修改。共享文件关联项目、模型和待确认项，不依赖同一聊天或暂停中的执行流程。

已有结果时再次要求生成，先识别用户是创建新方案还是修改当前方案，不静默覆盖现有结果。首次生成失败可恢复仍有效的分析和切片。已有业务 draft 是 generate 的辅助输入，详见 [10](10-draft-inputs.md)。

## 2. 固定开场：确定新旧项目并收集必要输入

开场交互固定完成两件事：确定新项目或旧项目；根据项目类型收集必要输入。已给出的项目类型直接复用，已提供的材料直接登记，只要求补充缺少的内容：

| 项目类型 | PRD | 高阶设计 | 原型 HTML/JS/CSS | 往期 SOW |
|---|---|---|---|---|
| 新项目 | 必需 | 必需 | 可选 | 不要求 |
| 老项目 | 必需 | 必需 | 可选 | 必需 |

除原型资源包外，只支持可直接读取的文本（默认 Markdown）与 Excel（首版 `.xlsx`）；PDF/DOCX、扫描件及其他格式暂不支持，要求提供相关可读内容，不自动转换。

高阶设计在本文简称 HLD。可额外提供 draft、tech note 或其他补充依据。开场不再固定询问用途、约束、范围和决策人；这些信息先从材料分析，真正缺少且影响推进时集中澄清。

先检查必需材料是否齐备及可读取。只有文件名、空壳模板或极少量标题不算资料充分。缺必需材料时直接说明应补什么，不能把原型、draft 或历史标题当作 PRD/HLD 的替代物。

## 3. 分析输入后：仅澄清和确认输入相关事项

agent 登记全部已提供输入，以往期 SOW 构建 as-is，以 PRD、原型、HLD 及其他项目材料构建 to-be；交叉分析业务、技术/NFR、交付的 gap、验收意图与责任边界。模板仅提供规则，不进入需求范围。主动识别公共能力及使用方、数据迁移和上线准备等本期工作，不等待材料提供现成的技术或交付标题。原型可选：没有原型不阻断；提供了相关原型则自主探索并明确实际观察与未验证内容。

分析后仅围绕输入中的业务、技术和交付事项整理待确认项、待决策项及需要补充的材料。提问说明来源、缺失或冲突、对输入理解的影响，以及需要用户回答的事实或作出的决定。可以解释输入中已有方案的差别，但不能虚构方案或替用户作决定。

| 输入事项 | 可以澄清或确认的内容 |
|---|---|
| 业务 | 本期范围、业务规则、验收意图，以及 PRD/原型中的歧义和冲突 |
| 技术 | 历史能力边界的具体冲突、HLD 方案含义及取舍、技术/NFR 约束、缺失技术事实；不例行追问历史是否已交付 |
| 交付 | 本期与外部责任、迁移范围及数据起点、发布条件、交付边界 |

这一阶段不要求用户批准 Epic/Feature/Story/Task 的组织和拆法、四项定性分类、切片安排或是否出稿。相关专业工作由 agent 完成。例如可以问“本次迁移有多少条记录”，不能把问题改成“请选这个 Task 的复杂度”。输入没有实质缺口就直接推进，不固定要求用户确认一遍分析摘要。Clarify 对具体修改方案的确认属于另一个入口，不受此处的提问范围替代。

| 情况 | 本次处理 |
|---|---|
| 必需材料缺失或基本业务/设计信息大量不足 | 要求补充资料，保留已完成分析，不进入生成 |
| 少量关键事实或决定可通过问答补足 | 集中交互；回答后仅复核相关结论和新增材料 |
| 回答后仍有可补足的关键缺口 | 还有一次补问额度且答案有实质进展时合批补问；到限仍缺基础就列补料清单结束，不反复追问 |
| 用户暂停、未补充或明确不知道关键事实 | 保留问题和分析，不用猜测、默认值或沉默批准向下推进 |
| 基本范围、验收意图与设计依据已足够支撑拆解 | 结束分析后澄清，进入分片生成 |

充分性由语义判断，不以问题数、页数或空值比例证明；执行另受 D04B 上限约束，默认首轮问题包加最多一轮补问。达到上限不代表资料已充分，基础仍不足就退出补料。用户提供判断所缺的业务、技术、交付事实与输入相关决定；agent 负责基于这些依据拆解和分类。

每轮只追问仍影响推进的内容，保留已答项。新答案改变结论时只复核关联范围，不重新分析整个项目。大量缺失用具体资料要求表达，避免用大量细碎问题试图凭空拼出 PRD 或 HLD。

## 4. Generate：整体骨架、逐片补全、合并出稿

信息补足后，agent 先按本期 gap 形成业务、技术和交付的 Epic/Feature 骨架、责任与依赖，再按关联切片联合形成 Story、来源 AC 与 Task。独立公共能力与业务接入分别界定工作、公共建设只计一次；迁移与上线准备进入交付层级。片内 Task 只做工作类型名称、工作方式、复杂度、集成类型的定性匹配；无相关候选时默认新建，候选实例适用未明时新建+待确认；实例及本次适用明确才按变化与标准判断，真正已满足且无工作不造 Task；人天交给模板。

切片可包含业务、技术或交付内容，粒度由关联与材料规模决定，不固定绑定 Feature、模型调用或子 agent。每片加载全局摘要、相关对象、证据及模板标准，局部检查后保存候选。合并时核对三类范围、公共建设与接入边界、交付责任及重复计量；只修相关内容。识别到必要工作但方案或责任缺依据时，按现有问答与留空规则处理，不为非业务需求另开审批阶段。

```mermaid
flowchart TD
    A["由 as-is / to-be 的本期 gap 形成骨架<br/>业务 / 技术 / 交付 Epic / Feature<br/>公共变化、消费适配与交付依赖"] --> B["选择关联切片<br/>可含业务、技术或交付内容"]
    B --> C["加载全局摘要与本片证据<br/>形成 Story 标题、来源 AC 与备注<br/>Task 分类：无候选新建；实例未定新建 + 待确认；适用明确按标准判断"]
    C --> D["局部检查并保存候选<br/>复杂度未定按 M 并附问题；其他缺值留空；复用候选按新建并关联问题<br/>按 D04B 批量处理，不新增逐片批准"]
    D --> E{"还有未完成切片？"}
    E -->|有| B
    E -->|没有| F["合并：三类范围覆盖<br/>公共建设 / 业务接入 / 交付责任<br/>检查遗漏与重复计量"]
    F --> G["生成并检查 Excel<br/>整体成功后保存可用版本"]
    H["明确错误：只修相关范围且计入共享返修次数<br/>到限 / 无进展退出，保留有效前序"]
    D -.发现错误.-> H
    F -.发现错误.-> H
```

图源：[按片生成](diagrams/generate-slices.mmd)。

分析时足够推进，不等于已预知所有细节。后期复杂度无法识别就默认 M，其他局部字段没有依据则保留空白，在备注中定位问题并随 Excel 交付待确认项；不新增逐步骤问答，不为定档回头补证，不猜前提或采用未确认方案。后续答案由 clarify 在确认的切片内补入。

若后期发现的是大范围基础不足，而非局部字段缺口，停止推进并返回具体缺失和已完成草案，不能靠大量空行或“待确认”包装为可用初版。技术错误和明确内容错误也不能包装成业务未知。

必要检查完成后直接返回原模板填值的 Excel 与同版待确认说明。局部空缺在输入字段及问题中如实表达，金额让模板自己处理；不要求所有待确认项关闭，也不增加估算完整性门禁。

## 5. Clarify：讨论具体修改后更新

Clarify 接收对待确认项的答复、自由建议和新材料。先读取当前业务文件和有关输入，明确新信息怎样影响 Story、来源 AC、Task 分类、备注及待确认项。

方案说明改哪些对象、依据是什么、哪些字段可以补入、哪些仍需留空、预期影响和最深回查范围。用户确认后执行；答案草案不提前变成有效决定，未确认方案不修改当前 SOW。分类变更引发的人天变化由模板试算，不由 agent 自行估数。

仍缺证据时按需要讨论；不能用猜测填满。执行发现需扩大范围或改变方案时，保留原版本并回到具体方案讨论。成功应用直接返回新 Excel 与剩余待确认项，无后置定稿。详见 [04](04-bounded-change-and-recovery.md)。

## 6. 完整流程图

澄清最多两轮主动提问，追加调查最多一批；分片首次覆盖有限目标，返修另有请求级上限。故障恢复只处理原失败位置及必要依赖；新信息/新路径还须满足剩余轮次和有限范围才能继续。统一规则见 [D04B](detailed/D04B-bounded-loops.md)。

```mermaid
flowchart TB
    U["用户选择入口"] --> E{"本次意图"}
    subgraph GEN["generate：独立的一次生成请求"]
        G0["开场 1：确定新项目或旧项目<br/>已有答案直接复用"]
        GI["开场 2：收集必要输入<br/>PRD、高阶设计；旧项目另需往期 SOW<br/>文本 / XLSX；原型资源包可选"]
        G1{"必需材料齐备<br/>且内容可分析？"}
        GN["列出具体缺失资料及用途<br/>本次不伪造完整 SOW"]
        G2["读取全部输入的相关用途<br/>文本 / Excel 定位；原型自主探索<br/>未读、不可读或冲突如实记录"]
        GA["往期 SOW → 估算 as-is<br/>新项目无历史可为空；旧项目缺件需补料"]
        GT["PRD / HLD / 原型 / draft → to-be<br/>业务、技术 / NFR、交付目标与责任"]
        GG["Agent 对照 as-is / to-be 识别本期 gap<br/>已满足且无工作不计入；模板仅提供规则"]
        G3{"分析全部输入及最新答复<br/>基础信息是否补足？"}
        GQL{"仍有主动提问轮次？<br/>首轮 + 最多一轮补问"}
        GQ["仅澄清和确认输入相关事项<br/>业务 / 技术 / 交付事实、冲突与决定<br/>首轮问题包 + 最多一轮补问，不审批拆解与分类"]
        GB["由本期 gap 形成 Epic / Feature 骨架<br/>公共能力、业务接入、迁移 / 上线<br/>明确本期与外部责任及依赖"]
        GS["选择关联切片并加载上下文<br/>业务 / 技术 / 交付可联合处理"]
        G4["联合形成本片 Story、来源 AC、备注<br/>公共建设与接入分别拆工作<br/>Task 分类：实例适用明确才按变化判断<br/>无候选新建；实例未定新建 + 待确认"]
        GC["复用候选：工作方式为新建<br/>具体历史候选与问题随 SOW 带出"]
        GP["复杂度无法识别：M + 待确认<br/>其他缺依据字段留空并关联问题<br/>不猜前提或方案，不为定档反复补证"]
        GL["局部检查并保存候选<br/>无新增用户批准，不单独交付半成品"]
        GR{"还有未完成切片？"}
        GM["合并检查三类 gap 覆盖及已满足说明<br/>公共建设只计一次、接入不漏<br/>交付责任、依赖与汇总一致"]
        G5{"基本信息仍充分<br/>仅有可定位的局部缺项？"}
        G6["保留模板计算、检查与复读原四表<br/>D 列完整 AC；目标行 open 备注/范围行"]
        GE["保存有效版本并返回 Excel<br/>generate 结束"]
    end
    subgraph CLA["clarify：用户另行发起的一次调整请求"]
        C0{"共享文件中存在可用基线？"}
        CN["说明缺失或不兼容文件<br/>恢复基线或请用户先使用 generate"]
        C1["读取相关模型、问题及证据<br/>定位业务 / 公共能力与接入 / 交付影响"]
        C2["交互讨论具体方案<br/>写对象、依赖、回查深度、变化影响"]
        C3{"用户是否确认当前方案？"}
        CQ{"仍有本次方案修订额度？<br/>首份方案 + 最多一轮修订"}
        CS["保留原版本<br/>保存草案或结束讨论"]
        C4{"基线及确认条件仍有效？"}
        C5["应用确认切片<br/>禁止擅自扩大写范围与回查范围"]
        C6{"能在已确认方案内完成？"}
        C7["必要检查、局部重算、生成 Excel<br/>待确认项随应用结果同步更新"]
        CE["提交一致的新版本并返回 Excel<br/>clarify 结束"]
    end
    subgraph ERR["两入口共用的故障原则"]
        T0["技术故障 / 明确内容错误<br/>保存有效前序，定位最小修复范围"]
        T1{"有明确修法、剩余返修次数<br/>且仍在本次边界内？"}
        T2["仅修相关对象或失败工具步骤<br/>成功则续接原失败位置"]
        T3["无进展或范围不再成立：退出本次应用<br/>保留上次有效版本，返回具体诊断"]
    end
    F[("共享文件<br/>输入 / 依据 / 模型 / 待确认项 / 当前版本")]
    OUT["用户拿到 Excel 和待确认项<br/>可以直接离开，无定稿步骤"]
    E -->|首次生成| G0
    E -->|反馈调整| C0
    G0 --> GI
    GI --> G1
    G1 -->|否| GN
    G1 -->|是| G2
    G2 --> GA
    G2 --> GT
    GA --> GG
    GT --> GG
    GG --> G3
    G3 -->|缺少大量基础资料| GN
    G3 -->|少量关键输入问题| GQL
    GQL -->|有额度且下一问题包有价值| GQ
    GQL -->|到限 / 无实质进展| GN
    GQ -->|收到答复或补充，复核相关分析| G3
    GQ -->|未补充或用户暂停：不向下推进| GN
    G3 -->|信息已补足| GB
    GB --> GS
    GS --> G4
    G4 -->|候选实例 / 适用未定| GC
    GC --> GL
    G4 -->|其他局部缺值| GP
    GP --> GL
    G4 -->|无新增未决项| GL
    GL --> GR
    GR -->|有| GS
    GR -->|没有| GM
    GM --> G5
    G5 -->|否| GN
    G5 -->|是：已有依据内容及局部留空| G6
    G6 --> GE
    GE --> OUT
    C0 -->|否| CN
    C0 -->|是| C1
    C1 --> C2
    C2 --> C3
    C3 -->|未确认 / 取消| CS
    C3 -->|已确认| C4
    C4 -->|相关内容已变化| CQ
    C4 -->|有效| C5
    C5 --> C6
    C6 -->|需改变方案 / 扩大切片| CQ
    CQ -->|有且新范围可闭合| C2
    CQ -->|到限 / 无进展| CS
    C6 -->|可以；缺依据字段留空并记录| C7
    C7 --> CE
    CE --> OUT
    G2 -.读取.-> F
    GL -.保存本片草案.-> F
    GE -.写入.-> F
    C1 -.读取.-> F
    CE -.写入.-> F
    G2 -.故障.-> T0
    GB -.骨架错误.-> T0
    G4 -.本片内容错误.-> T0
    GL -.本片检查失败.-> T0
    GM -.跨片检查失败.-> T0
    G6 -.检查 / 导出失败.-> T0
    C5 -.故障.-> T0
    C7 -.检查 / 导出失败.-> T0
    T0 --> T1
    T1 -->|是| T2
    T1 -->|到限 / 无修法 / 同因无进展| T3
    T2 --> T4{"原失败位置"}
    T4 -->|generate 输入分析| G2
    T4 -->|generate 骨架局部修正| GB
    T4 -->|generate 本片内容| G4
    T4 -->|generate 本片检查| GL
    T4 -->|generate 跨片检查| GM
    T4 -->|generate 文件检查| G6
    T4 -->|clarify 切片应用| C5
    T4 -->|clarify 文件检查| C7
    OUT -->|用户有反馈，另行发起 clarify| C0
```

图源：[完整流程](diagrams/workflow.mmd)。

## 7. Generate 协作时序

时序图约束用户接入、共享文件与交付的协作顺序。阅读、探索和片内组织由 agent 自主决定。Clarify 独立时序见 [04](04-bounded-change-and-recovery.md)。

```mermaid
sequenceDiagram
    autonumber
    actor U as 用户
    participant G as generate agent
    participant T as 分析与文件工具
    participant F as 共享文件
    participant X as 模板投影与必要检查
    participant O as 用时与 token 记录
    U->>G: 要求生成初版 SOW
    G->>O: 开始 generate 请求
    opt 开场 1：项目类型尚未给出
        G->>U: 这是新项目还是旧项目？
        U->>G: 项目类型
    end
    opt 开场 2：必要输入尚未收齐
        G->>U: 收集缺少的 PRD、高阶设计；旧项目另需往期 SOW；原型可选
        U->>G: 提供材料；可额外提供 draft 和 tech note
    end
    G->>T: 登记文本 / XLSX 与可选原型，检查必需材料与模板
    break 必需材料缺失或内容严重不足
        G->>U: 具体资料要求；不进入拆解
        G->>O: 记录资料不足出口
    end
    loop 按有限材料/主题首次覆盖，调查追加最多一批
        G->>T: 自主阅读、调查与可选原型探索
        T-->>G: 来源证据、覆盖及读取缺口
        G->>G: 往期 SOW 构建 as-is，其余项目材料构建 to-be；识别三类目标与责任
        G->>F: 保存索引与分析草案
    end
    G->>G: 对照两侧找本期 gap；已满足且无工作不计入
    loop 仍有关键输入问题且有实质价值，最多两轮主动提问
        G->>U: 仅提出输入相关的缺失事实、冲突、待决策项与所需补充
        U->>G: 澄清或确认输入相关事项，补充材料
        G->>T: 仅复核受影响 as-is / to-be / gap，判断是否已补足
        break 到限仍缺基础、无进展，或用户未补充 / 暂停
            G->>F: 保留已完成分析及尚缺内容
            G->>U: 不推进拆解，说明仍需什么资料
            G->>O: 记录等待与本次出口
        end
    end
    Note over G,U: 不审批拆解、分类或出稿；没有输入待澄清项就直接继续
    G->>G: 信息补足后按 gap 形成 Epic / Feature、公共变化与接入及交付依赖
    G->>F: 保存骨架和覆盖索引
    Note over G,U: 后期局部缺依据字段留空并随 SOW 交付，不猜前提和方案
    loop 按有限关联片首次覆盖三类范围，修正受 D04B 共享上限约束
        G->>F: 读取全局摘要、本片对象与依赖
        G->>T: 按需补读本片证据及模板标准
        T-->>G: 可定位依据
        G->>G: 形成 Story 并整理来源 AC；区分公共建设、接入和交付 Task，做四项定性匹配
        alt 相关 as-is 匹配完成，未命中
            G->>G: 工作方式默认新建，不另问是否新建
        else 相关候选实例适用未明
            G->>G: 按新建出稿，附具体候选问题，不继续深挖
        else 实例及本期适用明确
            G->>G: 按实际变化与类型标准判断工作方式
        else 未读 / 本期基础有歧义 / 其他缺依据
            G->>G: 定向补读；后期局部未知留空并关联问题
        end
        G->>X: 检查结构、分类合法性与留空对应问题
        opt 明确局部问题，且同一候选与请求仍有返修次数
            X-->>G: 具体错误与关联对象
            G->>G: 只修本片和必要依赖
            G->>X: 续接本片检查
        end
        break 基础不足、到限仍有错误、技术失败或无进展
            G->>F: 保留已完成片及具体缺口
            G->>U: 返回缺口或诊断，不冒称完整交付
            G->>O: 结束请求并保留实际消耗
        end
        G->>F: 保存候选、留空原因与待确认项
        G->>O: 记录本片用时和可得 token
    end
    G->>X: 核对三类覆盖、公共建设与接入计量、交付责任；按模板生成并复读 Excel
    Note over G,X: 人天由模板计算；局部未知不当零，仅使用授权的新建/M口径并保留问题
    alt 必要检查通过，且缺项已如实表达
        G->>F: 激活模型、待确认项及 Excel 同一版本
        G->>U: 返回可用 Excel 和待确认项；无需定稿
    else 检查或文件失败
        G->>U: 具体诊断，保留有效草案
    end
    G->>O: 结束请求，记录结果与观测完整度
    Note over U,F: generate 已结束；用户提供后续答案时另行调用 clarify
```

图源：[Generate 时序](diagrams/generate-sequence.mmd)。
