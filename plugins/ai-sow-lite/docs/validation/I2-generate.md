# Generate 语义验证

状态：I2.1 文本/XLSX 读取与来源核对已完成实现、回归及独立审查；I1.1 前置候选探针保留如下。完整 generate Skill 的实际语义验收继续由 I2.2 推进。

## Three-scope 前置演练

以 EX01 的原始 PRD/HLD 文本制作独立临时项目；被测 Agent 不继承开发聊天，不获得设计案例、预制候选或 expectations。当前仅给专业边界指引，真实 Skill 与全部机械通道尚未实现。使用新项目、无历史 SOW、无原型的输入。

输入分析正确识别两页及查询 API、独立公共客户端及消费方边界、资料迁移内含核验、发布方案与外部执行责任。保留只读查询、不得自动重试副作用操作、迟到响应、排除平台重建等限制，没有追问记录条数来定档。

实际合批提出两项输入问题：本期交付责任，以及禁止直接访问生产前提下开发验证数据和核验结果的提供责任。第一项按 EX01 的 A1 答复；第二项是本次演练实际发现的有效输入缺口，评估者显式补充“客户提供脱敏样本和非生产环境，本期开发团队自测并提供核验结果，生产执行仍由客户负责”。它不是从原 HLD 推断出的既定事实。

续接原 Agent 后，自主生成2个 Epic、3个 Feature、5个 Story、7个 Task、3项依赖；条数只记录实际结果，不作为固定拆解要求。业务查询、公共技术能力及迁移/发布交付均有对应工作；公共程序库只建设一次，两页面各自接入；迁移核验内含，未增造平台、PoC、正式验收或生产执行。迁移为 M，1项复杂度问题保持 open；两项输入问题按实际答复 resolved。

可见 AC/备注保留查询行为、手动重试、迟到响应、只读范围、外部提供资产、迁移回滚和发布责任。副作用自动重试禁令完整保存在来源依据中，可见备注写为“不新增自动重试要求”；I2 应进一步核对关键否定约束在输出中的直接表达，不能仅依赖依据文件可回查。缺少资料查询到迁移的显式依赖边，但迁移 AC 保留历史资料可查义务；I2 的关系覆盖演练仍需核对这种修改导航缺口。

首次机械核对发现两项程序处理问题：macOS 项目根别名与已解析路径不一致被误判越界；单文件 source locator 显式指定已登记路径被一概拒绝。按错误归因修复校验器并补测试，不要求 Agent 重写业务。修复后复核 exit=0、diagnostics=[]、valid_for_render=true、unknowns_count=1；原候选、业务、原件和模板字节完全未改，没有 current 或 Excel。实际程序进程约107.13毫秒，非性能统计基准。该结果不代表完整 generate 或全部语义验收已通过。

观测记录见 [宿主验证](host-support.md)；材料与计量身份见 [脱敏探针记录](I1.1-probe-observations.json)。

实际原字节候选与来源保存在 [合成样本](samples/three-scope/README.md)，用于审阅和重放机械校验；不得作为后续真实生成的输入答案。

## I2.1 · 物理输入与来源核对

本项扩展文本/XLSX 原件登记、工作簿目录和所选区域读取，保留合并、隐藏、附注、类型化原值及公式缓存有无。公式不执行，原文件不重算或改写。相同原件和选择可复用读取，目录与区域身份分开；正式来源复用同一确定性摘录核对方式。

独立于实现侧测试夹具的公共 CLI 探针，使用只含 Lite runtime/scripts/contracts/assets 的复制目录和另外创建的两 Sheet 工作簿，已核对重复登记、目录/区域 read_id、数值0与布尔false的真实缓存、无缓存公式、隐藏附注和原字节不变。它不调用 Lite 测试辅助或内部函数。冻结后的同一探针再次通过；正式输出模板 SHA-256 仍为 6abc55d44bc66476a60c2251e18c0dfdb66709e07539c246dfdec3a0373f5332。首次独立审查发现元数据超出返回预算、注音混入正文、既有 BOM 兼容性以及无 dimension 合并区域四项反例，已按同一批定点修正并通过复审。以下完整回归属于修正前冻结快照，不能冒称已证明修正后的行为。

稀疏历史夹具包含跨行/合并、缺 AC/类型、隐藏附注、取消/未来范围和仅供查阅的标准目录。读取器只提供事实和定位；是否是历史范围、具体实例是否适用等专业结论，由 I2.2 新会话演练验证，本项不代为判断。

| I2.1 验证 | 实际结果 |
|---|---|
| XLSX 首个 TDD 纵向 | 真实旧入口 FORMAT_UNSUPPORTED 导致1项失败；实现后同例通过 |
| 来源接缝 TDD | 8项分析/AC/历史来源检查从 OPERATION_UNSUPPORTED 失败到通过 |
| 有限读取与目录反例 | 类型、BOM、边界、附件、游标/用途、UTF-16 XML实体等分别先观察失败再修正；原已支持行为如实记首次通过 |
| 冻结前聚焦输入/合同 | 197 passed，64.90秒 |
| 冻结后完整 Lite | 397 passed、1 skipped，361.59秒；跳过项为本机缺少 PowerShell |
| 仓库根测试 / 验证器 | 42 passed；repository validation passed |
| 旧插件完整回归 / 独立副本 | 539 passed、4 skipped，50.19秒；副本 setup/generate 均为 OK |

上述测试只证明各自检查范围，不能代替新 Skill 的语义验收，也不是实际 SOW 生成性能基线。缓存夹具曾因 `<v />` 与 `<v></v>` 的形式差异注入失败，修正夹具后重跑；不将其记为产品缓存故障。输入 source_readers 的迁入保留已有 ZIP/维度边界与结构方法，补类型/原字面量、缓存存在性及确定性摘要；没有迁入旧业务充分性或提取引擎。

### I2.1 独立审查后的定点修正

四项反例在一个返修批次内处理：整份查询结果包含元数据在内统一受返回预算约束；字符串正文排除注音；按旧登记编码保留 BOM 附件/摘要含义；实际可读范围包含合并边界。超大元数据明确返回 RESULT_TOO_LARGE，保留完整区域附件，不能用 complete=true 隐藏未返回内容。

修正前11项定点断言失败，修正后11项通过；其中历史成员数量的测试曾未真正超限、错误响应曾误要求没有既有观测字段，已分别修正夹具规模和测试断言，不把它们记为生产故障。最终输入/XLSX/全部校验合同聚焦208项通过（69.14秒），修正后的独立复制 smoke 通过，实际一次 Office 转换；31份 Python 读取审计收据未发现越界。审计粒度沿用 AD09 的已知限制。

18份本项冻结源码/合同/夹具/参考文件与最终测试字节一致，父任务的独立公共 CLI 探针再次通过。没有重复全部 Office 套件：此前397通过/1跳过仍标为修正前完整回归，最终208项及复制 smoke 证明受影响范围；Office 实现和模板未改。独立定点复审为 Spec Pass / Quality Approved，F1—F4 全部关闭。复审者另行使用真实基线 CLI 重建 BOM 原例；四个原反例及共10组相关场景断言通过，11项窄回归通过，18份冻结文件字节一致。

提交暂存区检查另发现 Git 的 LF 规范化会改写三份 BOM/CRLF 原字节夹具。仅在历史夹具目录增加三条 binary 属性；先观察暂存字节不一致，再验证全部18份冻结文件与暂存区摘要一致，并从实际暂存区导出的独立 Lite 副本运行旧 BOM 回归通过。运行时和被复审源码字节未变。
