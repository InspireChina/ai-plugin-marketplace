# Generate 语义验证

状态：I2.1—I2.3 已完成，独立审查通过。I1.1 前置候选探针、首次失败、实际计量和未验证边界均保留如下；G12 的资源全覆盖目标仍开放。

## Three-scope 前置演练

以 EX01 的原始 PRD/HLD 文本制作独立临时项目；被测 Agent 不继承开发聊天，不获得设计案例、预制候选或 expectations。当前仅给专业边界指引，真实 Skill 与全部机械通道尚未实现。使用新项目、无历史 SOW、无原型的输入。

输入分析正确识别两页及查询 API、独立公共客户端及消费方边界、资料迁移内含核验、发布方案与外部执行责任。保留只读查询、不得自动重试副作用操作、迟到响应、排除平台重建等限制，没有追问记录条数来定档。

实际合批提出两项输入问题：本期交付责任，以及禁止直接访问生产前提下开发验证数据和核验结果的提供责任。第一项按 EX01 的 A1 答复；第二项是本次演练实际发现的有效输入缺口，评估者显式补充“客户提供脱敏样本和非生产环境，本期开发团队自测并提供核验结果，生产执行仍由客户负责”。它不是从原 HLD 推断出的既定事实。

续接原 Agent 后，自主生成2个 Epic、3个 Feature、5个 Story、7个 Task、3项依赖；条数只记录实际结果，不作为固定拆解要求。业务查询、公共技术能力及迁移/发布交付均有对应工作；公共程序库只建设一次，两页面各自接入；迁移核验内含，未增造平台、PoC、正式验收或生产执行。迁移为 M，1项复杂度问题保持 open；两项输入问题按实际答复 resolved。

可见 AC/备注保留查询行为、手动重试、迟到响应、只读范围、外部提供资产、迁移回滚和发布责任。副作用自动重试禁令完整保存在来源依据中，可见备注写为“不新增自动重试要求”；I2 应进一步核对关键否定约束在输出中的直接表达，不能仅依赖依据文件可回查。缺少资料查询到迁移的显式依赖边，但迁移 AC 保留历史资料可查义务；I2 的关系覆盖演练仍需核对这种修改导航缺口。

首次机械核对发现两项程序处理问题：macOS 项目根别名与已解析路径不一致被误判越界；单文件 source locator 显式指定已登记路径被一概拒绝。按错误归因修复校验器并补测试，不要求 Agent 重写业务。修复后复核 exit=0、diagnostics=[]、valid_for_render=true、unknowns_count=1；原候选、业务、原件和模板字节完全未改，没有 current 或 Excel。实际程序进程约107.13毫秒，非性能统计基准。该结果不代表完整 generate 或全部语义验收已通过。

观测记录见 [宿主验证](host-support.md)；材料与计量身份见 [脱敏探针记录](I1.1-probe-observations.json)。

实际原字节候选与来源保存在 [合成样本](samples/three-scope/README.md)，用于审阅和重放机械校验；不得作为后续真实生成的输入答案。

## I2.1 · 物理输入与来源核对

本项扩展文本/XLSX 原件登记、工作簿目录和所选区域读取，保留合并、隐藏、附注、类型化原值及公式缓存有无。公式不执行，原文件不重算或改写。相同原件和选择可复用读取，目录与区域身份分开；正式来源复用同一确定性摘录核对方式。

独立于实现侧测试夹具的公共 CLI 探针，使用只含 Lite runtime/scripts/contracts/assets 的复制目录和另外创建的两 Sheet 工作簿，已核对重复登记、目录/区域 read_id、数值0与布尔false的真实缓存、无缓存公式、隐藏附注和原字节不变。它不调用 Lite 测试辅助或内部函数。冻结后的同一探针再次通过；正式输出模板 SHA-256 仍为 6abc55d44bc66476a60c2251e18c0dfdb66709e07539c246dfdec3a0373f5332。首次独立审查发现元数据超出返回预算、注音混入正文、既有 BOM 兼容性以及无 dimension 合并区域四项反例，已按同一批定点修正并通过复审。以下完整回归属于修正前冻结快照，不能冒称已证明修正后的行为。

稀疏历史夹具包含跨行/合并、缺 AC/类型、隐藏附注、取消/未来范围和仅供查阅的标准目录。读取器只提供事实和定位；是否是历史范围、具体实例是否适用等专业结论，由 I2.2 新会话演练验证，本项不代为判断。

| I2.1 验证 | 实际结果 |
|---|---|
| XLSX 首个 TDD 纵向 | 真实旧入口 FORMAT_UNSUPPORTED 导致1项失败；实现后同例通过 |
| 来源接缝 TDD | 8项分析/AC/历史来源检查从 OPERATION_UNSUPPORTED 失败到通过 |
| 有限读取与目录反例 | 类型、BOM、边界、附件、游标/用途、UTF-16 XML实体等分别先观察失败再修正；原已支持行为如实记首次通过 |
| 冻结前聚焦输入/合同 | 197 passed，64.90秒 |
| 冻结后完整 Lite | 397 passed、1 skipped，361.59秒；跳过项为本机缺少 PowerShell |
| 仓库根测试 / 验证器 | 42 passed；repository validation passed |
| 旧插件完整回归 / 独立副本 | 539 passed、4 skipped，50.19秒；副本 setup/generate 均为 OK |

上述测试只证明各自检查范围，不能代替新 Skill 的语义验收，也不是实际 SOW 生成性能基线。缓存夹具曾因 `<v />` 与 `<v></v>` 的形式差异注入失败，修正夹具后重跑；不将其记为产品缓存故障。输入 source_readers 的迁入保留已有 ZIP/维度边界与结构方法，补类型/原字面量、缓存存在性及确定性摘要；没有迁入旧业务充分性或提取引擎。

### I2.1 独立审查后的定点修正

四项反例在一个返修批次内处理：整份查询结果包含元数据在内统一受返回预算约束；字符串正文排除注音；按旧登记编码保留 BOM 附件/摘要含义；实际可读范围包含合并边界。超大元数据明确返回 RESULT_TOO_LARGE，保留完整区域附件，不能用 complete=true 隐藏未返回内容。

修正前11项定点断言失败，修正后11项通过；其中历史成员数量的测试曾未真正超限、错误响应曾误要求没有既有观测字段，已分别修正夹具规模和测试断言，不把它们记为生产故障。最终输入/XLSX/全部校验合同聚焦208项通过（69.14秒），修正后的独立复制 smoke 通过，实际一次 Office 转换；31份 Python 读取审计收据未发现越界。审计粒度沿用 AD09 的已知限制。

18份本项冻结源码/合同/夹具/参考文件与最终测试字节一致，父任务的独立公共 CLI 探针再次通过。没有重复全部 Office 套件：此前397通过/1跳过仍标为修正前完整回归，最终208项及复制 smoke 证明受影响范围；Office 实现和模板未改。独立定点复审为 Spec Pass / Quality Approved，F1—F4 全部关闭。复审者另行使用真实基线 CLI 重建 BOM 原例；四个原反例及共10组相关场景断言通过，11项窄回归通过，18份冻结文件字节一致。

提交暂存区检查另发现 Git 的 LF 规范化会改写三份 BOM/CRLF 原字节夹具。仅在历史夹具目录增加三条 binary 属性；先观察暂存字节不一致，再验证全部18份冻结文件与暂存区摘要一致，并从实际暂存区导出的独立 Lite 副本运行旧 BOM 回归通过。运行时和被复审源码字节未变。

## I2.2 · 初版 Generate 真实演练

实现、真实演练、回归和最终独立复审已完成。开发副本只包含插件运行面、合同和当前案例的原始资料；没有开发设计、测试、期待文件、预制模型或其他会话回答。由一个新的主 session 自主调用 Skill，需要澄清时只接收实际给出的用户答复。多个测试会话不共享专业上下文，Office 由测试控制者串行调度；该设备等待不属于用户批准或插件的正常阶段，性能统计单独排除。首个 three-scope 在调度补充到达前已完成 Office/apply，实际没有设备等待。

[Generate 案例](../../tests/fixtures/generate/cases/README.md) 和 [历史案例](../../tests/fixtures/history/cases/README.md) 将输入、答复、期待分开保存。评价依据是实际义务、来源、范围和分类，不要求固定命名、层级数量或 Task 数。完整合成工作目录留在本次本地验证工件中，公开记录只保留脱敏结果与文件身份。

先前无 Skill 的对照仅给 PRD/HLD，要求专业文本草稿，未给模板或规则，也未生成 Excel。它已正确识别三类工作和公共归属，但询问了迁移记录条数。这只支持核对默认 M 指引的必要性，不能把已有专业能力全部归因于新 Skill，也不能作为完整耗时改善的对照。

入口静态独立审查没有 Critical/Important，发现两处机械字段映射歧义：文本区域定位来自 coverage.selector.locator，XLSX 区域来自 coverage.locator；标准行 ID 来自“工作类型 ID”。已明确字段并以真实返回结果执行映射测试；没有相关真实 actor 失败，不能把这项说明改进记为已修复语义故障。rev1 已运行案例的其他专业指引与 rev2 相同。

### 已观察的场景与首次缺陷

| 场景 | 当前实际结论 |
|---|---|
| 缺 HLD / 仅 draft 与 tech note | 两个新会话均读取已有资料后列出具体 PRD/HLD 缺件并结束，项目无工作簿或零版。 |
| 两轮输入问题后仍缺方案 | 采用实际答复的同步查询事实；一次集中补问后仍缺契约、方案选择和责任，保存补料清单结束。新会话仅凭文件及 inspect/request 续接，保持已耗次数、不重问。 |
| three-scope | 版本 270df8d2-ab0b-4c2f-84ff-197606427780 交付成功，5 Story / 7 Task；三类工作、公共单计、迁移内含核验、M 问题和四项依赖正确。发布 AC 出现 S1，完整语义验收待定向复验。 |
| local-nfr | 版本 58eb529a-6023-4243-b94b-9b38ef988e7c 交付成功，4 Story / 6 Task；页面内 NFR 保留且无公共库，迁移默认 M。发布 AC 同样出现 S1，迁移备注出现 S2。 |
| sparse-history | 首次 full check 通过后 render 遇 XLSX 来源标签 KeyError；修复后沿原请求交付版本 9beb3406-8231-43fc-bf86-4e63f3fc090f。API 与事件均新建且保留具体历史候选问题，隐藏附注、排除和目录区分正确。 |
| confirmed-instance | 版本 fbaeadf0-272e-49a8-bc4e-cda6421ee2b9 成功交付。唯一既有查询事务按实际 status 过滤变化选调整/S，未知契约细节随版待确认，无额外复用、平台或交付工作。 |
| already-satisfied | 版本 8aa46647-0e56-44ac-a53d-d4ba14c4d67a 成功交付。三份材料明确同一实例及全部目标已满足，三类 gap 均空；未造占位层级或 Task，待确认 0。 |
| 重复 generate | 新会话读取已生效请求/current，返回 three-scope 原版本；10 份版本文件及 current 字节完全未变，没有再生成或增加版本。此项只验证重复调用，不改变该版本已记录的 S1。 |

S1 是把估算标准中的通用发布成果清单写入项目 AC，原材料只要求沿用既定六步骤、回滚及 Go/No-Go。S2 是在只收到责任答复 A1 的情况下，把客户提供且允许用于验证的数据写成已知前提；原材料没有 A2 的验证数据答复。rev3 在同一处来源规则中约束 AC/备注：标准用于 Task 单位、包含边界和分类；未知事实、责任及验证前提保留为具体问题。原已生效版本未修改；使用[有限交付范围变体](../../tests/fixtures/generate/cases/delivery-source-boundaries/inputs/)复验共同根因。

真实 XLSX 故障发生在 Office 启动前。修复依据来源 kind 显示文本行号或 Sheet/单元格范围，并将内部 render 实现修订加入失败续接身份；投影数据合同、模板及业务字节保持原值。旧失败只有在实现确实变化且原返修额度允许时继续；旧成功准备包仍须完整验证才复用。实际原请求 render 共两次、返修一批、apply 一次，候选修改零次，没有删除旧 attempt 或重置次数。

### 定点复验与最终回归

`delivery-source-boundaries` 从新的原始材料副本成功交付版本 bc1c47aa-145a-4850-a564-c9117e0b8dba。发布 AC 只保留材料的组件、团队、窗口、既定六步骤、回滚、Go/No-Go 和核验引用；迁移备注没有认定客户提供或授权验证数据，而将可用输入和提供方式绑定为待确认。迁移 M 及其问题立即采用，核验内含；没有新增生产执行或独立测试资产。该结果关闭 S1/S2 的受影响范围，不改写前两份首次产物或把它们全部标为首次通过。

`multiple-query-instances` 成功交付版本 ef0e81cc-e82d-47b8-90de-5dd1d9926967：同一订单对象的详情与列表仍是两个独立查询事务，均新建/S；具体响应封装和普通分页细节留为一项问题，没有自行编造默认值。

另用既有 I1 机械夹具建立两次同根因计算失败的续接状态：故障注入在 Office 边界，实际没有启动 Office 进程，第三次程序调用被 LOOP_LIMIT_REACHED 拒绝。新会话只看到保存文件，经真实 inspect 核对 draft/current 后停止；候选字节、已耗一次 render 重试和返修批次不变，没有再 render/apply。这验证耗尽后的执行行为，不作为专业生成或真实 Office 故障的证据。

独立安装的实际缺陷也在本项修复：uv 默认 Python 安装曾创建一条指向首个测试副本的用户 bin 链接，后续副本借用该解释器。只删除本次创建且目标完全匹配的链接；自举改为禁止用户 bin/注册表写入、选择本副本 managed Python，并在已有虚拟环境引用外部副本时重建自身环境。真实双副本测试验证第一份移除后第二份仍可运行；PowerShell 执行环境未验证。

最终复审发现另一个安装边界反例：`.venv` 自身为目录链接时，uv 的 clear 会清空副本外的目标。离线真实 uv/Python 反例确认测试目标的原字节会被删除；仅使用自有临时目标，没有触碰用户环境。补丁在 mkdir、安装、clear、sync 之前拒绝 `.venv`、`.ai-sow-tools` 及其 bin/cache/python 根目录的链接或重解析点，包括悬空形式；保留链接和目标，返回 BOOTSTRAP_PATH_UNSAFE。测试覆盖外部哨兵和原字节、未执行 uv，以及正常 managed 版本别名和真实副本启动。该检查只限五处写入根目录，不递归扫描运行环境。

| 验证 | 最终证据 |
|---|---|
| 缺入口 TDD | 11 failed → 139 passed；精确返回映射补充后 141 passed |
| XLSX 来源/续接 TDD | 2 failed → 聚焦通过；真实原请求随后一次重试交付，候选不改 |
| Bootstrap TDD | 4 failed → 11 passed、2 skipped；含实际双副本和外来虚拟环境恢复 |
| 路径保护 TDD / Bootstrap 与入口聚焦回归 | 11 failed、10 skipped → 36 passed、12 skipped，38.64 秒；包含真实 uv 的目录链接反例，跳过项为缺少 PowerShell |
| 路径保护前完整 Lite | 427 passed、2 skipped，419.39 秒；含真实 Office 和旧成功准备包复用，不冒充保护补丁后的完整套件 |
| 路径保护前 Lite 独立副本 | 通过，实际 Office 一次；31 份 Python 读取审计收据、零越界，临时目录已清理；审计粒度沿用 AD09 限制 |
| 路径保护后 Lite 独立副本 | 通过，实际 Office 一次；31 份审计收据、零越界，副本已清理；模板及业务投影未改 |
| 最终暂存区根检查 | 42 passed，2.365 秒；repository validation passed，差异格式检查通过，四份路径保护冻结文件与暂存字节一致 |
| 旧插件完整回归 / 独立副本 | 539 passed、4 skipped，50.76 秒；setup/generate 均成功 |

原模板 SHA-256 仍为 6abc55d44bc66476a60c2251e18c0dfdb66709e07539c246dfdec3a0373f5332。阶段取舍见 [AD11—AD13](implementation-decisions.md)。完整首例的原生计量已留存，I2.3 接入时区分响应观察、物理调用和实际活动，不将成功写入 Excel 等同于性能达标。

同一独立审查者已接受字段映射、XLSX 来源消费修复、S1/S2 实际复验与有限续接证据；最后对路径保护作定点复审，结论为规范 PASS、质量 APPROVED，无遗留 Critical/Important/Minor。此后只同步公开状态，不重复审查整个阶段或重新生成案例。

## I2.3 · 首次真实性能基线

此处保留 I2.2 首次 three-scope 的成本，不把修正后的指引冒充该次运行版本。它已交付 5 Story / 7 Task，但含上文 S1；不能用来声称同质量优化成功或相对旧版已提速。原生来源已在 actor 结束后完整复核；项目计时按该次结束边界筛选，排除了后来重复 generate 的两次只读查询。

| 范围 | 实测值 | 解释与限制 |
|---|---|---|
| 原生响应观察 | 31 条；input 2,337,786，output 23,925，total 2,361,711 token | 无重复、冲突或算术不符；不是 31 次物理调用，也不是峰值上下文 |
| 缓存 / 推理明细 | cached input 2,245,120；reasoning output 3,555 | 已包含在 input/output 中，不再相加 |
| 单响应输入量 | 首条 29,021、末条 111,862 token，分别为本次最小和最大值 | 是宿主报告的该条响应输入量，可观察增长；不把累计 2,337,786 当作峰值上下文 |
| 两个原生处理轮次 | 211,859 ms 与 606,951 ms，合计 818,810 ms | 含宿主和工具活动，不是纯模型计算时间 |
| 轮次间隔 / 用户等待标记 | 原生间隔 29,215 ms；活动标记间隔 55,611.342 ms | 起止位置不同，不能直接从处理轮次扣除整段标记或宣称精确人类思考时间 |
| 32 次 CLI 工具计时 | ingest 5 次 79.152 ms；inspect 21 次 191.214 ms；check 4 次 119.190 ms；render 1 次 5,842.115 ms；apply 1 次 1,151.391 ms | 同进程时钟配对；不含 CLI 启动前自举、外层 shell 或模型时间 |
| 32 次 CLI 信封 | 输入 12,718、输出 47,082 UTF-8 字节 | 规范化业务信封口径，不能换算 token |
| 外层工具调用 | 29 条调用与 29 条返回对应；提交代码 66,799 字节，返回块紧凑 JSON 216,214 字节 | 一个外层调用可以包含多个 CLI；返回块序列化大小不是 UI 展示或模型计费大小 |
| 输入 / 标准读取 | 来源目录 3 次、区域 13 次，无重复的相同选择器；标准目录 1 次及三个不重叠的类型集合 | 原 PRD/HLD 在登记前已直接读过，后续区域用于定位和摘要；本例没有历史材料 |
| 返修 / 重算 | 补充答复的 analysis 登记出现一次 EVIDENCE_MISSING，有限补入四份已有依据原值后通过；三个生成片均无候选返修，render/apply 各一次 | 不遗漏失败成本，也不把合并文件复制当作 Agent 重生成 |
| 合并业务文件 | model 40,035、pending 977、decisions 1,133、candidate 1,329 字节 | 最终文件量；不能据此还原所有中间写入量 |

实际可用文件里程碑存在；request start/end 和首次有用反馈标记缺失，不能补写过去时刻作为实测。参考文件各读取一次，artifacts Schema 出现一次完整读取和一次必要定义摘取；这为后续减少无关合同内容提供线索，但尚没有同质量对照或独立因果实验。代码内工具总时长远小于两个宿主轮次的总时长；仅凭这种差异不能把剩余时间全归为“模型思考”。

原生白名单读取的本次观察耗时 11.89 ms，另一次工具成本摘取耗时 5.158 ms；它们只是开发观察脚本的运行时间，不能冒充产品采集器开销。I2.3 继续验证版本适配、观测降级、真实取消边界和新增标记；I4.3 再作不同规模与改稿的统一对照。[宿主矩阵](host-support.md) 保留各项实测与未知。

补充的 `existing-unsupported-hld` 使用 I2.2 提交 100d588 的运行面副本：新会话沿用旧项目身份，一批要求往期 SOW 及可读取的 HLD，明确 PDF 不支持，保存 input-readiness.md 后结束，没有候选、current、工作簿或零版。该 PDF 仅为扩展名拒绝夹具，不声称有效 PDF 解析；本例在 ingest 前停止，也不作为自举或完整生成的证据。案例输入/期待继续隔离。

原生 collector 已通过真实 CLI 接入同一份冻结记录：31 条响应、两个轮次的六项计数与原白名单逐项一致，request coverage 保持 partial，物理调用、纯模型耗时和精确活动归属仍未知。首次 collector 76.433 ms、含启动的 CLI 167.860 ms；重放 collector 14.253 ms、CLI 104.336 ms。第一次读取 1,412,114 字节，重放只读 24,046 字节的身份/尾部核对区域，事件身份与报告来源摘要稳定，不重复加总。来源原字节未变；临时观测项目已清理，不拷贝 transcript，也未修改原已交付项目。这是一次实际接入与一次重放的开销，不是统计性能保证。初次探针误用脱敏 turn 别名而被拒绝，改用精确授权文件中实际身份后取得上述结果；不把重试抹掉或把 EOF 当作请求完整。

### I2.3 实现验证

原生读取、去重/冲突、容量与游标、迟到/写入失败、无效计数后缀及埋点示例均按 TDD 逐项推进：相应 Red 为 2、4、11、2、3 项失败，修复后最终聚焦 114 passed、119 deselected，2.58 秒；这些过程数字不可相加充作独立测试总数。新定义只在 telemetry 合同范围，既有业务合同不变。

真实采集子进程在第一条 usage 已 fsync 后收到 OS SIGINT，退出 130；已有计量前缀和游标保留，业务字节不变。此项证明 Python 采集进程的取消传播，不证明宿主能中断模型或 Office；重放由独立游标测试和上述真实二次采集验证。

| 冻结后检查 | 结果 |
|---|---|
| 完整 Lite | 461 passed、12 skipped，403.70 秒；跳过项为缺少 PowerShell |
| Lite 独立副本 | 通过，真实 Office 一次，31 份 Python 审计收据、零越界，副本已清理；审计边界沿用 AD09 |
| 仓库根测试 / 验证器 | 42 passed，2.068 秒；repository validation passed |
| 旧插件完整回归 / 独立副本 | 539 passed、4 skipped，46.93 秒；setup/generate 成功，自有临时副本已清理 |
| 冻结与暂存 | 七份拥有文件与测试冻结摘要、暂存字节一致；原模板摘要未变；差异格式检查通过 |

独立审查结论为 Spec PASS、Quality APPROVED，Critical/Important/Minor 均为零。G12 的原生定位、请求全覆盖和精确逐活动 token 缺口仍保留，不能将这组机械测试当作资源目标全部达成。此后只同步公开状态和最终检查事实，不重复整阶段审查。
