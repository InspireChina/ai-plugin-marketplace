# I1 可靠程序交付验证

I1.1 已完成实现、TDD 回归及独立审阅收口。I1.2 输入登记/版本保存已完成实现、TDD 回归及独立审阅/定点复核。I1.3 Excel 投影已实现，通过完整回归及原生检查，独立审阅中；I1 整体尚未完成，I1.4 埋点运行时、I1.5 完整副本交付仍未实现。

## I1.1 已实现与证据

以设计提交 `311fee2` 为起点，在当前分支新增 Lite 自有隔离环境、6份 JSON Schema、严格 JSON/摘要、只读候选校验和 `scripts/lite.py --request`。校验器负责真实来源/标准身份、分类和问题一致性、引用闭合、历史去向及路径边界；不会拆解业务、改专业字段或计算金额。

I1.1 提交 `423234b` 时只有 check/candidate 可执行，其余操作明确返回 OPERATION_UNSUPPORTED。contract_case 是直接准备的真实字节单元夹具；未伪造 ingest 成功、current 或 Excel 交付。I1.2 增加的能力见本文末节，当前接口见 [工具合同](../../references/tools.md)。

| 验证 | 实际结果 |
|---|---|
| 测试先行 | 真实 fixture 准备后，核心实现不存在时63项失败、1跳过，无 fixture/收集错误；后续路径别名、显式 locator、内容摘要等修复分别保留失败再通过的证据 |
| 锁定依赖 | uv lock、uv sync --locked、uv lock --check 通过；Python3.12.13、jsonschema4.26.0、openpyxl3.1.5、pytest8.4.1 |
| Lite 全量 | 首轮95 passed、1 skipped；审阅返修后144 passed、1 skipped；唯一跳过为缺少 pwsh，未宣称 Windows 执行通过 |
| 隔离环境 | Bash 自举在含空格/中文路径的临时 Lite 副本真实重建并复用 .venv，实际转发 UTF-8 请求；从不同 cwd 调用不依赖仓库环境 |
| 平台与编码 | macOS26.5.2 arm64 实测，Bash 无 BOM、PowerShell UTF-8 BOM；路径样例283字符/387 UTF-8字节，不作为平台最大长度或 Windows 支持承诺 |
| 原模板 | assets SHA-256 保持 `6abc55d44bc66476a60c2251e18c0dfdb66709e07539c246dfdec3a0373f5332`；既有模板测试原字节不变，包含原 LibreOffice 回归 |
| 真实 Agent 候选 | 两轮生成后以独立运行时副本实际调用 check CLI，通过、1项 open、无业务改写；见下方探针记录 |
| 已保存样本复现 | 将下方合成样本复制到另一临时项目，补原模板，再用独立运行时副本校验：通过、1项 open，14份已有文件原字节不变、未创建 current |
| 仓库边界 | 根42项测试、仓库验证器、旧插件独立副本 smoke 通过；I1.1 初测曾有2项旧架构断言失败，后续专项修复后旧插件全目录539 passed、4 skipped |

复核命令：

```text
uv sync --project plugins/ai-sow-lite --locked
uv run --project plugins/ai-sow-lite --locked pytest plugins/ai-sow-lite/tests -q
sh -n plugins/ai-sow-lite/scripts/bootstrap.sh
```

严格摘要向量 `{"a":[1,true,null],"z":"中文"}` 对应 `json-v1:db6a0f2ebe94b34e63a8bbcc767de7852280476696e69e1152bdbb07a6f3cf32`；保留业务字符串空白、数组顺序和精确整数。运行时不接受外部 Schema 取回或复制旧领域规则。

旧插件全目录曾有两项既有失败：`test_runtime_is_plugin_shared_owner_agnostic_infrastructure` 的精确文件清单未包含已有 findings.py；`test_all_professional_owners_freeze_owner_local_candidate_first_interface` 的指定文字断言未匹配已有 generate-task 指引。I1.1 提交后按用户要求专项修复了过期清单与措辞断言，保留 Owner 边界、页序、已读页不重复读取和截断恢复检查；旧插件运行时与 Skill 原字节未改。先复现2 failed，再验证架构12 passed、全目录539 passed/4 skipped，根42项、仓库验证器与独立副本 smoke 均通过。

## 独立审阅与定点返修

独立审阅确认了6项机械反例：历史条目的实例依据/父链、重复主题版本、覆盖记录与输入集合矛盾、逆向历史替换链、尾随换行的 UUID，以及无关 Schema 错误遮蔽分类诊断。已逐项补失败与通过样例并修复。独立复核核对原反例、合法情形及最终文件身份后，Spec 和 Quality 均通过，6项问题均关闭；I1.1 已勾选完成。

测试先按合同定义预期，再运行旧实现观察失败，随后修复并回归。下表的失败数是各组新增反例在修复前的实际结果，不是人为注入 fail 标志；对应测试位于 [test_contracts.py](../../tests/test_contracts.py)。

| 预期来源 | 新增反例 | 修复前证据 |
|---|---|---|
| [D02](../design/detailed/D02-shared-data-and-evidence.md) 历史实例依据与显式父引用闭合 | 嵌套依据缺失、父项缺失、重复身份、父链循环；另有合法稀疏历史 | 4 failed、1 passed |
| D02 稳定主题版本身份 | 同一主题版本重复或冲突，保留合法共享情形 | 4 failed、1 passed |
| [D03](../design/detailed/D03-input-analysis-and-exploration.md) 输入与覆盖记录一致 | 未登记来源、非法定位顺序、主题输入与覆盖不符；保留合法未读区域 | 5 failed、1 passed |
| D02 历史替换去向与版本定位 | 逆向或缺乏后续关系的替换链；保留合法链与有据删除 | 4 failed、2 passed |
| [P00](../design/implementation/P00-contracts-and-fixtures.md) UUID/摘要精确编码 | 六份 Schema 与实际候选的尾随换行身份 | 7 failed |
| [I1.1](../design/implementation/P01-reliable-delivery.md) 按实际依赖合批诊断 | 无关文件格式错误遮蔽分类等诊断，非法文件不能冒充空集合 | 8 failed、2 passed |

修复后合同测试127项通过，聚焦环境/合同135项通过、1跳过，全量144项通过、1跳过。这证明已列机械预期与反例在当前实现上成立，不代表自然语言义务全部覆盖。真实 Agent 探针的专业缺口仍单独记录在 I2，不能以程序测试通过关闭。

另有同一 Schema 文件内重复定义的非阻断维护建议，已使用现有 $defs/$ref 做局部去重。重构前9项行为保护测试通过，重构后相关23项通过，再纳入全量测试；这些原本通过的保护测试不冒称 Red。没有引入生成框架或业务阶段。

## 前置探针与后续工作

[Generate 验证](I2-generate.md) 保留真实三类义务、默认 M、输入问答、可见约束及关系导航限制；[宿主验证](host-support.md) 保留生产者版本、真实 response/turn 计数、耗时与归属未知。原字节 [合成候选](samples/three-scope/README.md) 可用于复核。探针没有安装 Skill、调用模型 SDK 或修改宿主配置，不能替代 I2 端到端验收。

I1.2 把直接合同夹具接到真正的输入登记及可靠文件保存；继续保留 Agent 专业自由、有限修改、原模板计算和诚实观测边界。I1.3 的实际 Excel 投影与核验接入见末节。

## I1.2 文本登记与有效版本保存

本轮消费 P00/D07 的文件接口：真实文本与分析登记、选择性读取、checkpoint、不可变准备包保存及事实恢复。XLSX 读取在 I2.1、原型适配在 I4、真实 Excel 投影在 I1.3、clarify 确认验证在 I3；当前不能用模拟的确认或 Office 记录声称后续能力已完成。

| 验证预期 | 检查边界 |
|---|---|
| 原件与身份可复查 | 外部明确材料进入项目副本后保留原始字节与换行；用途/版本一致时可复用；项目类型不能被续接输入改写 |
| 部分失败不丢资料 | 同批有效输入保留并可定位，失败输入给出诊断；登记不创建 current |
| 局部读取有明确范围 | 查询结果与分页数字一致，游标绑定查询和来源版本，过期或零匹配不自动扩查 |
| 有效版本完整切换 | 锁外准备，短锁内复核；current 原子替换是生效点，未切指针的目录不视为已应用 |
| 中断与重复请求不重复写入 | 各指定切点用子进程故障验证；同意图命中原结果，不倒回当前指针；不同意图拒绝复用 ID |
| 次数与取消有真实边界 | 丢失的计数不按零重建；已观察取消阻止迟到应用，宿主 UI 信号是否传达仍单独限定 |

独立公共 CLI 验收在实现前已于“首次真实 ingest 应成功”断言失败，实际结果为 I1.1 的 OPERATION_UNSUPPORTED。该验收只通过 CLI 和磁盘原件判断结果，不导入生产模块或复用测试 helper；覆盖中文路径、CRLF、重复/部分失败、身份、分页、恢复和子目录链接逃逸。

文本登记实现后，这组验收的10次真实子进程调用全部符合预期：重复输入复用身份，同批缺失文件不丢有效资料，项目类型冲突不改原登记，新增输入使旧分页游标失效，草案恢复不创建 current，项目子目录链接逃逸被拒绝。验证使用临时独立运行时代码副本、不同 cwd 和现有锁定 Python 解释器；不等于完整独立安装或 Excel 交付。此次单次 CLI 历时为68.40—95.90ms，只是本机小样本观测，未采集 token，也不作为性能承诺。

首轮实现文件冻结后，再次运行同组独立验收并核对10个实现文件摘要，全部通过。正文读取按选择范围与游标返回，current/objects/standards/request 使用真实项目内容；不支持的格式、视图或选择器明确拒绝。

同步文档后，根测试发现 D07 新链接少了一层相对路径，修正后42项通过；仓库验证器、已跟踪/新增文件空白检查及原模板摘要检查通过。旧插件在 `4bd4d49` 专项修复后保持原字节，沿用本轮已通过的539项测试、4项跳过与独立副本 smoke 证据。

| TDD 切片 | 实际 Red | Green 与收口 |
|---|---|---|
| 真实登记、部分失败、身份及 checkpoint | 8 failed、1 passed | 9 passed |
| analysis、真实来源与有限读取 | 9 failed、6 passed | 两文件18 passed |
| 短锁、完整版本、历史幂等、取消与五个中断点 | 14 failed、5 passed | 存储19 passed |
| current/objects/standards 与游标边界 | 6 failed、15 passed | 两文件47 passed |
| 摘录附件、重复索引、封闭协议、损坏恢复 | 4 failed、48 passed | 两文件52 passed |
| 旧主题不得误报新依据已保存 | 1 failed、1 passed | 2 passed；合法复用继续通过 |
| 已有原件时不得把丢失索引重建为空 | 1 failed | 1 passed |

另有公共核验接缝和响应故障测试；其中一次失败来自设备号测试替身缺字段，修正替身后验证，未将它算成产品缺陷。上述 Red 来自当次实际工具输出；原有已通过的保护测试不冒称先失败。首轮独立审阅前，聚焦输入/存储为 **55 passed（19.43s）**，完整 Lite 为 **199 passed、1 skipped（37.02s）**；唯一跳过仍为缺少 PowerShell。原有 I1.1 测试全部保留并通过。

```text
uv run --project plugins/ai-sow-lite --locked pytest plugins/ai-sow-lite/tests/test_inputs.py plugins/ai-sow-lite/tests/test_project.py -q
uv run --project plugins/ai-sow-lite --locked pytest plugins/ai-sow-lite -q -rs
```

五个中断由真实子进程在测试屏障处被终止：完整准备后、版本目录保存后、指针替换前，recover 都为 draft；指针替换后、成功响应前，recover 都为 applied。进程终止后锁可再次取得；旧请求在后续串行版本之后仍返回原 applied_version，不倒回 current。没有生产 failpoint、自动重试循环或并行改稿成功场景。测试中的 sow.xlsx 是明确标识的受控字节包，证明文件事务，不证明 Office 交付。

macOS 本地文件系统实测 flock、进程退出释放、文件/目录刷新与原子替换。Windows 锁分支已有实现但未实测；跨设备拒绝为受控设备号模拟，未宣称真实跨盘、网络盘或断电持久性。取消只对已收到的本地信号生效，不承诺宿主 UI 信号必然传达。

I1.2 交付时公共 Generate apply 复核准备文件、候选、基线和摘要，但缺少实际 `workbook.verify_prepared`，因此返回 OPERATION_UNSUPPORTED。I1.3 现已接入最终 Excel/投影/真实 Office 核验及字节绑定，并以真实包验证公共成功路径，不能凭自填成功标志生效；Clarify 的方案/确认验证仍在 I3。I1.2 不改变模板、估算规则、专业流程、依赖或旧插件。

独立审阅逐文件核对10份实现的摘要及基线差异，确认4个可复现问题：

| 项目 | 反例与影响 | 返修范围 |
|---|---|---|
| 分析登记中断 | 主题文件保存后索引写入失败，重试误报登记成功，但主题查询零命中 | 复用前核实登记完整性；只处理同一合法候选的缺失登记，不自动重建分析 |
| 同字节文件改名 | 复用原物理身份，却合入新 basename 的定位，返回的区域无法读取 | 定位绑定实际保存的原件，保留严格路径检查 |
| 轻量查询多读 | 单项 current 查询仍读取全部原件和工作簿 | 查询仅核对指针/manifest 与选中文件；apply/recover 保留完整性检查 |
| 问题文本检索 | 已允许的问题 title 查询未检索 question 字段，真实问题零命中 | 按问题文本检索，保留正确零匹配 |

前两项阻断 I1.2 关闭，后两项作为同范围的查询正确性与读取成本修复一并处理。四项均已完成测试先行的定点修正：

| 返修组 | 修复前实际结果 | 修复后实际结果 |
|---|---|---|
| 分析索引中断及字节/引用不符 | 5 failed | 5 passed |
| 改名重复导入与返回定位 | 1 failed | 1 passed |
| 窄读取、选中文件/历史链损坏、完整恢复及问题检索 | 7 failed、3 passed | 10 passed |

原本已通过的3项保护不冒称 Red。修复只改 inputs.py、project.py、对应输入测试及工具说明；其余6份受审文件原字节不变。重试仅补同一合法分析候选的缺失索引成员，不重建整个索引；改名材料仍复用原件和身份，未保存别名仍拒绝。查询返回明确的 `verification_scope`，current 只验证指针/manifest，objects 验证有效链和所选文件；分页 complete 不表示整包交付核验。原件或工作簿损坏仍会被 apply/recover 拒绝。

最终聚焦测试 **71 passed（30.38s）**；完整 Lite **215 passed、1 skipped（47.29s）**，唯一跳过为 PowerShell 不可用。修复后再以独立代码副本执行原10次公共 CLI 验收，全部通过，并核对最终实现字节；不把修复前结果冒充新版本验证。独立定点复核确认 Spec 的两项阻断及 Quality 的两项问题全部关闭；复核快照、原始测试日志和最终 CLI 证据均匹配最终文件。I1.2 已勾选完成，I1 整体保持未完成，下一项为 I1.3。

## I1.3 Excel 投影与真实交付（已完成）

I1.2 已提交为 `a5b19c4`，本项在该提交上继续。范围是原模板投影、独立问题/长文说明、真实 Office 计算和最终文件核验，并接通已有公共 apply；I1.4 观测及 I1.5 完整独立安装验收保持后续任务。

独立公共 CLI 验收使用临时运行时代码副本、不同 cwd 和合成数据文件，不导入生产模块或测试 helper。实现前，真实来源登记、区域读取、分析登记及 full check 已成功，随后在 render 返回 OPERATION_UNSUPPORTED 的断言处失败。该 Red 记录来自真实执行；后续同一验收脚本已通过，检查结果见下文。

真实 LibreOffice 26.8.0.3 对未改模板副本的原始往返退出码为0：五张 Table 的名称、范围、样式与筛选保留，十个计算列元数据省略；Story 校验范围从整列裁至1064行，Task 裁至1204行。解码后的保护标志相同，数组公式 TRUE/TRUE() 是等价保存表达。由此明确 [D06 §8](../design/detailed/D06-excel-projection-and-delivery.md#8-office-复读失败与恢复) 的两项有限兼容处理，禁止扩展成任意元数据修复或改公式/缓存。

Microsoft Excel 16.112.3 对原始 Office 输出副本的预检没有出现修复提示，四张 Sheet、冻结和基础布局可见；关闭未保存。这只是适配预检，不能代替实际生成结果的打开、保存、重开验收，也不能据此宣称 Table 元数据完整。原模板资产未操作，实际交付记录见下文；独立审阅结果见末尾。

实施中的真实 TDD 记录：投影切片14 failed →14 passed；Office 切片11 failed →11 passed（31.79s）；公共交付链11项在实现前失败。Office 的初次修复仍有8 failed/3 passed，第二次才全部通过，未把中途文件名为 green 的日志误记成通过。综合结果和原生输出验证已追加于下文；独立审阅结论见末尾。

独立公共 CLI 的首轮集成验收已通过30次真实子进程调用：渲染前未创建 current，首版应用及重复应用/恢复身份一致；重复 render 返回同版文件且原字节未变；16份输入、读取附件、分析和模板依赖保持原字节。该小型样本首次 render 为5.291s、复用1.291s、apply1.360s、重复 apply0.124s、recover0.089s，仅是当前 Mac 的单次观测，不代表整体 Generate 性能或已实现 I1.4 埋点。

原生检查发现中文备注在窄列中实际换行更多，初版 Story E5 行高78.75点截住第二个依赖目标。已通过单独失败样例修正行高估计与窄列长文出口，未改模板列宽、字体、原文或计算列。新版采用保守留白；长 AC 保存原文片段与 details 入口，长任务列表保留 H 列公式并在备注指向完整正文。

新版常规样本和61 Story/201 Task 扩行样本均在 Microsoft Excel16.112.3 实际打开、保存副本、关闭并重新打开，未出现内容修复提示。复核 Story AC/依赖、Task 默认 M/待确认正文、特殊字符别名、长 AC 与完整任务列表入口可读。保存后独立逐单元格检查：常规样本3,506个非公式单元格、1,304个公式及其数组范围；扩行样本3,518个非公式单元格、1,314个公式及其数组范围，内容均与原生成文件一致；四张 Sheet、保护、Table 范围和10个计算列元数据保留。原生成文件与基础模板未被原生保存操作改写，只有专用 QA 副本改变字节。最终代码快照综合测试已通过；独立审阅结果见末尾。

首轮修复前冻结实现的综合证据（最终返修结果见后文）：

| 验证 | 实际结果与范围 |
|---|---|
| Lite 全量 | `pytest -c plugins/ai-sow-lite/pyproject.toml plugins/ai-sow-lite/tests -q`：266 passed、1 skipped，188.65s；唯一跳过为 PowerShell 不可用 |
| Excel/Office/原模板 | 指定三文件先59 passed；新增 tint 损坏反例修复后，最终全量包含当前60项三文件测试；22个 office 标记用例全部执行，无 Office 跳过 |
| 旧插件规定检查 | 根42项、仓库验证器通过；`plugins/ai-sow/skills` 388 passed、4 skipped；旧插件独立复制 smoke 的 setup/generate 均 OK；旧插件原字节不变 |
| 独立公共 CLI | 冻结后同一脚本30次调用全部通过；18份运行时、脚本、合同及资产与当前源码原字节一致；不导入生产模块或测试 helper |
| 本机单样本用时 | 冻结后首次 render5.485s、复用1.140s、apply1.176s、重复 apply0.110s、recover0.076s；仅记本次观察，不作性能阈值或 token 结论 |
| 原生保存重开 | 两个 v2 副本全部通过；精确输入、公式与数组 ref、保护和 Table 保存比较见上文 |

测试命令的一次 collect-only 调用曾未指定 Lite 配置/目录，误收集旧插件同名测试并出现收集错误；随后采用显式配置和目录得到22/267个 office 用例。该命令错误不算产品缺陷，也不当作通过证据。样本里的无 gap 夹具也曾字段类型写错，订正夹具后验证，没有把它记为业务实现的 Red。

冻结前另有真实反例：Office 核验后来源发生变化、数值样式关联名、CRLF 原文保存和 tint 样式损坏。均保留先失败的测试与修复后结果，纳入最终全量。`verification.json` 记录本地执行事实与实际字节绑定，不是引擎签名证明；最终核验不计算人天，也不声称抵抗能完整重写项目和所有收据的操作者。Windows 进程清理/启动未实测，完整独立 Lite 安装验收仍属于 I1.5。

独立源码审阅核对12份实际实现文件，发现两项需要定点修正的问题：

| 发现 | 实际反例 | 修复与验收要求 |
|---|---|---|
| 显示元数据漏检 | 在真实 Office 原始输出中仅隐藏 Story 第5行，或将字体名称改为 Wingdings，封存仍通过 | 将行可见性及有效字体纳入复读，未知损坏拒绝；不增加恢复这些属性的适配 |
| 中等任务列表没有全文出口 | 一个 Story、短 AC、空备注，关联三项较长任务；H 列内容所需高度超过实际63.75点行高，但没有 details 入口 | 将列表显示需求与本行可用高度衔接，不足时调整行高或提供完整列表；保留 H 原公式，再补该样本的原生检查 |

审阅未重跑完整套件，仅对具体疑问作定向探针。既有 v2 两包的原生证据有效，不能代替三任务边界；两项按同范围的测试先行返修，结果如下。

| 返修切片 | 实际 Red | Green 与边界 |
|---|---|---|
| 行可见性及字体 | 六种真实 raw 损坏均未被旧实现拒绝，6 failed | 6种损坏全部拒绝，正常 raw 继续通过；另5项字体例外限制首次即通过，不算 Red |
| 三任务中等列表 | 真实 render 成功，但无可达全文入口，1 failed | 与其他相关布局合计5 passed；短 AC/空备注时也生成独立全文 |
| 长备注与列表入口组合 | 追加列表入口导致同一全文锚点冲突，1 failed | 组合及中等列表2 passed；保留两个独立入口和原文 |

严格字体比较发现正常引擎会替换前三张表的固定 A2 说明字体。一次限定环境探针未保持原字体，因此按 [D06 §8](../design/detailed/D06-excel-projection-and-delivery.md#8-office-复读失败与恢复) 接受精确坐标与字体对的只读例外：Calibri/minor → Arial Unicode MS/无 scheme，其余字体属性、文本及 minor 主题必须相同。它是已观察到的字体替换，不是主题等价；业务格、其他字体对或隐藏行仍拒绝。未增加 OOXML 恢复、字体安装或模板修改。

中等样本的 Story 行高从63.75点改为117.75点，E 有完整任务列表入口，details 保留三项完整名称；H 的数组公式和原始引擎缓存不改。原生检查曾因 Excel 无响应中断，用户重启后补齐：100%显示下 E 入口和 H 三条任务可读，打开、保存副本、关闭、重开均无修复提示。保存后的3,506个非公式单元格、1,304个公式与数组范围比较无差异，Table 名称/范围/计算列元数据存在性及锁定保留。只有 QA 副本保存，原生成文件摘要不变。

返修最终两文件聚焦测试 **64 passed（192.78s）**；完整 Lite **279 passed、1 skipped（259.87s）**，唯一跳过仍为缺少 PowerShell。与首次审查快照相比只改5份实现/测试/工具说明文件，其余7份受审文件保持原字节。冻结后独立副本的30次公共 CLI 调用再次全部通过，18份运行时/脚本/合同/资产均与最终源码一致。该次本机 render6.039s、复用1.347s、apply1.383s、重复 apply0.175s、recover0.100s；不当作性能阈值，也不声称已经采集 token。独立定点复核已通过，原两项问题全部关闭，未发现新增问题。

最终根测试42项通过，仓库验证器及差异空白检查通过；原模板摘要仍为 `6abc55d44bc66476a60c2251e18c0dfdb66709e07539c246dfdec3a0373f5332`，旧插件及原模板测试保持原字节。本轮完成 I1.3 并保留未提交改动，下一项为 I1.4 观测；I1 整体验收、完整 Skill 交付及 Windows 实跑未冒称完成。
