# 资料查询原型

这是自包含的合成 HTML/JS/CSS 原型，页面与交互都只使用本地演示数据，不连接真实后端。可以直接打开 index.html；需要 HTTP 入口的浏览器可在本目录执行 `python3 serve.py`，读取输出的本机地址。使用完成后在该进程中按 Ctrl+C；不要终止其他服务或浏览器。

页面包括资料列表、资料 A001 详情、查询和修订备注。部分源码分支没有接入当前入口。原型表现不证明生产环境能力，也不替代 PRD 与高阶设计；未观察到的行为须保留观察限制。
