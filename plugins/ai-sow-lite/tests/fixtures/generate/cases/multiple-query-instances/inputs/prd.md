# 本期订单查询 API

## p-detail
这是新项目，本期直接交付订单详情查询 API。调用方按订单编号精确查询，只读返回订单编号、状态和金额；没有匹配时返回空结果。

## p-list
另行交付订单列表查询 API。调用方按销售编号精确筛选，以简单分页返回订单编号和状态；没有匹配时返回空列表。详情和列表是两个分别调用、分别验收的接口，没有页面开发范围。

## p-boundary
两个接口仅查询同一订单业务对象；不含订单写入、复杂计算、排序聚合或其他本期功能。
