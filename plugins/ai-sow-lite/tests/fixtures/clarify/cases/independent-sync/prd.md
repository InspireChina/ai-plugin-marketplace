# 订单状态出站集成 PRD
本期新项目，一个内部订单系统向客户既有合作方系统推送订单状态。订单状态变更后发送订单编号、状态、状态发生时间、客户编号、地区代码、版本号六字段；只传指定订单，不增加页面、入站查询或其他业务域。成功保留合作方确认标识；失败显示在既有运行日志内并沿既定人工重试流程处理。取消与回滚业务状态不在本次新增范围。验收由客户确认给定成功、失败后人工重试两类脱敏例子的字段和反馈符合双方既有规范。
取数、六字段映射及传输是这项出站集成的内部实现，只交付一个出站集成成果；无独立调度、版本、同步程序或独立验收。
