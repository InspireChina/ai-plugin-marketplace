# 售后订单访问 · 本期 PRD

## p-query
售后人员按订单号只读查询当前订单的编号、状态和金额；查询无结果时明确返回空结果，不创建或改变订单。归档订单检索不在本期范围。

## p-event
当前订单状态发生变化时向客户 CRM 发送状态通知，包含订单编号、新状态和变更时间。CRM 只消费状态变化，不消费完整订单明细；不要求历史补发。

## p-boundary
本期只有上述查询与状态通知能力，未安排退货、对账、数据迁移或新的上线准备交付。
